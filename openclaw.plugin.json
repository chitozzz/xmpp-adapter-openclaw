{
  "id": "xmpp",
  "name": "XMPP",
  "description": "XMPP/Jabber channel plugin for OpenClaw",
  "channels": ["xmpp"],
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "channelConfigs": {
    "xmpp": {
      "schema": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string" },
          "enabled": { "type": "boolean" },
          "jid": {
            "type": "string",
            "description": "Bot JID (e.g. bot@example.com)"
          },
          "password": { "type": "string" },
          "host": {
            "type": "string",
            "description": "XMPP server host (optional, auto from JID)"
          },
          "port": {
            "type": "integer",
            "default": 5222
          },
          "tls": { "type": "boolean", "default": true },
          "mucNick": {
            "type": "string",
            "description": "Nick in MUC rooms (default: localpart of JID)"
          },
          "homeChannel": {
            "type": "string",
            "description": "MUC room for cron/notifications (optional)"
          },
          "allowFrom": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Allowed bare JIDs (empty = nobody, use pairing)"
          },
          "dmSecurity": {
            "type": "string",
            "enum": ["allowlist", "pairing", "open"]
          },
          "uploadService": {
            "type": "string",
            "description": "XEP-0363 upload service JID (e.g. share.example.com). Optional: auto-discovered via disco#items/info"
          },
          "allowInsecureTls": {
            "type": "boolean",
            "default": false,
            "description": "Accept self-signed TLS certs when uploading to the share host (internal Snikket/Prosody only)"
          }
        },
        "required": ["jid"]
      },
      "uiHints": {
        "jid": { "label": "JID", "help": "Bot JID, e.g. bot@example.com" },
        "password": { "label": "Password", "help": "Account password", "sensitive": true },
        "host": { "label": "Server", "help": "Optional; auto-discovered from JID" },
        "port": { "label": "Port", "help": "Default 5222" },
        "mucNick": { "label": "MUC nick", "help": "Nick in group chats" },
        "allowFrom": { "label": "Allow from", "help": "Allowed bare JIDs" },
        "uploadService": { "label": "Upload service", "help": "XEP-0363 file upload service JID (optional)" },
        "allowInsecureTls": { "label": "Allow insecure TLS", "help": "Accept self-signed certs on the upload host" }
      }
    }
  }
}
